# .github/workflows/audit.yml (FINAL COMPLETE VERSION)
name: Professional SEO Audit

# Trigger the workflow manually with input fields
on:
  workflow_dispatch:
    inputs:
      audit_url:
        description: 'URL to audit'
        required: true
        default: 'https://example.com' # FIXED: Added default URL
      audit_level:
        description: 'Audit level (basic, standard, or advanced)'
        required: true
        default: 'standard' # FIXED: Added default level
        type: choice
        options:
        - basic
        - standard
        - advanced
      competitor_url:
        description: 'Competitor URL for comparison (Optional)'
        required: false
      audit_scope:
        description: 'Crawl scope'
        required: true
        default: 'full_site_250_pages' # FIXED: Added default scope
        type: choice
        options:
        - only_onpage
        - onpage_and_index_pages
        - full_site_250_pages

jobs:
  run-audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        
      - name: Set up Python (VERSION FIXED) ✅
        uses: actions/setup-python@v5
        with:
          # Changed from '3.10' to '3.9' for reliable installation on GitHub runners
          python-version: '3.9' 
          
      - name: Install system dependencies for Playwright
        # This fixes the 'libgconf-2-4' and other missing package errors.
        run: |
          sudo apt-get update
          sudo apt-get install -y libglib2.0-0 libnss3 libfontconfig1 libcups2 libxtst6 libxss1 libgbm-dev
          
      - name: Install dependencies and corpora 🛠️
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt --upgrade
          # Install Playwright browser drivers required by Scrapy-Playwright
          playwright install --with-deps
          # Downloads the necessary data for textblob and textstat 
          python -m textblob.download_corpora
          
      - name: Run SEO Audit 🚀
        env:
          AUDIT_URL: ${{ github.event.inputs.audit_url }}
          AUDIT_LEVEL: ${{ github.event.inputs.audit_level }}
          COMPETITOR_URL: ${{ github.event.inputs.competitor_url }}
          AUDIT_SCOPE: ${{ github.event.inputs.audit_scope }}
        run: |
          python main.py
          
      - name: Upload SEO Audit Reports
        uses: actions/upload-artifact@v4
        with:
          name: seo-audit-reports-${{ github.event.inputs.audit_level }}
          path: reports/
          retention-days: 5
          

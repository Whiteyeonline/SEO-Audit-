requests
beautifulsoup4
lxml
tldextract # Note: Not used in current checks, but often useful.
markdown # Not strictly necessary if only generating MD, but helpful.
validators
textblob
textstat # New dependency for content quality
pypiwin32 # Dependency for textstat if running on Windows

# Requirements for the Python SEO Audit Tool

# Core Web Scraping/Crawling
scrapy>=2.11.0
scrapy-playwright>=0.19.0 # Ensures modern JS rendering support

# Data Processing and Analysis
requests>=2.31.0
textblob>=0.18.0
nltk>=3.8.1
textstat>=0.7.3 # Used for readability checks
Pillow>=10.0.0

# HTML Parsing and Manipulation
beautifulsoup4>=4.12.3
lxml>=4.9.4 # Highly recommended fast parser for BeautifulSoup

# File Handling (if needed, but good practice)
filelock>=3.13.1

# requirements.txt - EXPERT VERSION (FINAL FIX)

# Core Web Scraping/Crawling
scrapy>=2.11.0
# FIXED: Using latest version, removing strict non-existent constraint
scrapy-playwright
playwright>=1.47.0
# Explicitly including Twisted for stable Scrapy performance
Twisted>=22.10.0

# Data Processing and Analysis
requests>=2.31.0
textblob>=0.18.0
nltk>=3.8.1
# 🔥 CRITICAL FIX: Add the missing package for readability checks
textstat>=0.7.1 

# HTML Parsing and Manipulation
beautifulsoup4>=4.12.3
lxml>=4.9.4

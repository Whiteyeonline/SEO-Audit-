# requirements.txt - EXPERT VERSION (FIXED)

# Core Web Scraping/Crawling
scrapy>=2.11.0
# 🔥 FIX: Removed non-existent version constraint. 
# This line will now install the latest stable scrapy-playwright (e.g., 0.0.44).
scrapy-playwright
playwright>=1.47.0
# Explicitly including Twisted for stable Scrapy performance
Twisted>=22.10.0

# Data Processing and Analysis
requests>=2.31.0
textblob>=0.18.0
nltk>=3.8.1

# HTML Parsing and Manipulation
beautifulsoup4>=4.12.3
lxml>=4.9.4
